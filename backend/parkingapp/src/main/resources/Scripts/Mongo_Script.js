// Conectar a la base de datos GestionEmpleadosDB
use GestionEmpleadosDB;

// Crear la colección Access (vacía)
db.createCollection("Access");

// Crear la colección Employee (vacía)
db.createCollection("Employee");